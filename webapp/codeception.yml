actor: Tester

bootstrap: _bootstrap.php

coverage:
  enabled: true
  show_only_summary: false
  whitelist:
    include:
      - models/*

paths:
  data: tests/_data
  helpers: tests/_support
  log: tests/_output
  tests: tests

settings:
  memory_limit: 1024M
  colors: true

modules:
  config:
    Yii2:
      configFile: 'config/test.php'
